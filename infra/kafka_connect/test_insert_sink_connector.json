{
  "name": "test_insert_sink_connector",
  "config": {
    "connector.class": "io.confluent.connect.jdbc.JdbcSinkConnector",
    "tasks.max": "1",
    "connection.url": "jdbc:postgresql://test-postgres:5432/auth?sslmode=disable",
    "connection.user": "sony",
    "connection.password": "secret",
    "topics": "test-insert",
    "table.name.format": "test_message",
    "insert.mode": "insert",
    "pk.mode": "none",
    "auto.create": "true",
    "auto.evolve": "true",
    "max.retries": "3",
    "retry.backoff.ms": "1000"
  }
}
